fastapi==0.115.5
uvicorn[standard]==0.32.1
pydantic==2.10.3
pydantic-settings==2.6.1
python-multipart==0.0.20
httpx==0.28.1
replicate==1.0.4
openai==1.57.4
groq==0.13.0
Pillow==11.0.0
python-dotenv==1.0.1
aiofiles==24.1.0

# ── Perception (local CV) ──────────────────────────
# Heavy: install separately on the perception host. App falls back to
# heuristic stubs if these are absent (enable_perception still True).
numpy==2.2.0
opencv-python-headless==4.10.0.84
ultralytics==8.3.40          # YOLOv8 + FastSAM
torch==2.5.1                 # CPU build for perception; CUDA build for local SD
timm==1.0.12                 # MiDaS depth via torch.hub
diffusers==0.31.0            # local Stable Diffusion + ControlNet
accelerate==1.2.1            # enables model_cpu_offload for 4GB VRAM
httpx==0.28.1                # already listed above, used in precache script too
