fastapi==0.115.5
uvicorn[standard]==0.32.1
pydantic==2.10.3
pydantic-settings==2.6.1
python-multipart==0.0.20
httpx==0.28.1
replicate==1.0.4
openai==1.57.4
groq==0.13.0
Pillow==11.0.0
python-dotenv==1.0.1
aiofiles==24.1.0

# ── Perception (local CV) ──────────────────────────
# Heavy: install separately on the perception host. App falls back to
# heuristic stubs if these are absent (enable_perception still True).
numpy>=1.26
opencv-python-headless>=4.9
ultralytics>=8.3             # YOLOv8 + FastSAM (no exact pin — avoids torch conflicts)
# torch: install MANUALLY with CUDA BEFORE running pip install -r requirements.txt
# pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121
timm>=0.9                    # MiDaS depth via torch.hub
diffusers>=0.30              # local Stable Diffusion + ControlNet
accelerate>=0.26             # enables model_cpu_offload for 4GB VRAM
