# Lightweight deps for a no-GPU cloud host (Render / Railway / Fly).
# Perception runs in stub mode (ENABLE_PERCEPTION=false); images via Replicate;
# brief + chat via Groq/OpenAI. No torch / ultralytics / diffusers / opencv.
fastapi==0.115.5
uvicorn[standard]==0.32.1
pydantic==2.10.3
pydantic-settings==2.6.1
python-multipart==0.0.20
httpx==0.28.1
replicate==1.0.4
openai==1.57.4
groq==0.13.0
Pillow==11.0.0
python-dotenv==1.0.1
aiofiles==24.1.0
